# AISE 4030 Assignment 2 — Policy Gradient configuration
# Agent selection: "reinforce" or "ppo"

agent_type: "ppo"

environment:
  name: "BipedalWalker-v3"
  seed: 42

shared:
  gamma: 0.99
  hidden_sizes: [64, 64]
  activation: "tanh"
  max_grad_norm: 0.5
  device: "auto"

reinforce:
  policy_lr: 0.0003
  value_lr: 0.001
  max_episodes: 3000
  moving_average_window: 100
  checkpoint_interval: 10
  eval_episodes: 5
  # When true: standardize discounted returns G_t per episode before baseline subtraction for policy loss.
  normalize_returns: true
  return_norm_epsilon: 1.0e-8

ppo:
  lr: 0.0003
  rollout_length: 2048
  mini_batch_size: 64
  epochs: 10
  clip_epsilon: 0.2
  gae_lambda: 0.95
  value_loss_coef: 0.5
  entropy_coef: 0.01
  max_episodes: 20
  moving_average_window: 100
  checkpoint_interval: 10
  eval_episodes: 5

paths:
  reinforce_results: "reinforce_results"
  ppo_results: "ppo_results"

run:
  # Default paths; can be overridden via CLI for eval
  checkpoint: null
  render: false
